# Changelog

## 0.1.0

- Initial release candidate of `OnloSDK`.
- Includes the native messenger, protected identity/session state, durable
  outbox, configuration, media, push, and lifecycle recovery.

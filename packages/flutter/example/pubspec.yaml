name: onlo_flutter_example
description: Minimal host example for the Onlo Flutter plugin.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  onlo_flutter:
    path: ../

flutter:
  uses-material-design: true
